import { msg } from "@lingui/core/macro";

export const ErrorBoundary = {
  DEFAULT_TITLE: msg`Section Display Error`,
  DEFAULT_DESC: msg`An unexpected error occurred while rendering this component.`,
  TRY_AGAIN: msg`Try Again`,
};
